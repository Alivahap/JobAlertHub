## Packages
framer-motion | Smooth page transitions and layout animations

## Notes
Authentication uses a mock JWT stored in localStorage.
Authorization header 'Bearer <token>' must be included in requests.
Service Worker logic is client-side only for this mock.
Uses existing shadcn/ui components found in client/src/components/ui.
Tailwind config should support the custom government-style color palette.
